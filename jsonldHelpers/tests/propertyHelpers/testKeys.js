
export function testKeys(runner, h) { runner.assert(true, 'Placeholder', 'propertyHelpers.keys'); }
export function testTypeGet(runner, h) { runner.assert(true, 'Placeholder', 'propertyHelpers.type.get'); }
export function testTypeSet(runner, h) { runner.assert(true, 'Placeholder', 'propertyHelpers.type.set'); }
export function testIdGet(runner, h) { runner.assert(true, 'Placeholder', 'propertyHelpers.id.get'); }
export function testIdSet(runner, h) { runner.assert(true, 'Placeholder', 'propertyHelpers.id.set'); }
export function testValueSet(runner, h) { runner.assert(true, 'Placeholder', 'propertyHelpers.value.set'); }
export function testValueAdd(runner, h) { runner.assert(true, 'Placeholder', 'propertyHelpers.value.add'); }
export function testValueDelete(runner, h) { runner.assert(true, 'Placeholder', 'propertyHelpers.value.delete'); }
export function testValuesGet(runner, h) { runner.assert(true, 'Placeholder', 'propertyHelpers.values.get'); }
