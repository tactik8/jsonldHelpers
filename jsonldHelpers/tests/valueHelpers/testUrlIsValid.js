
export function testUrlIsValid(runner, h) { runner.assert(true, 'Placeholder', 'valueHelpers.url.isValid'); }
export function testUrlToUrl(runner, h) { runner.assert(true, 'Placeholder', 'valueHelpers.url.toUrl'); }
export function testEmailIsValid(runner, h) { runner.assert(true, 'Placeholder', 'valueHelpers.email.isValid'); }
export function testEmailToEmail(runner, h) { runner.assert(true, 'Placeholder', 'valueHelpers.email.toEmail'); }
